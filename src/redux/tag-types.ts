export enum tagTypes {
  category = 'category',
  admin = 'admin',
  user = 'user',
  product = 'product',
  shopper = 'shopper',
}

export const tagTypesList = [
  tagTypes.admin,
  tagTypes.category,
  tagTypes.user,
  tagTypes.product,
  tagTypes.shopper,
]
